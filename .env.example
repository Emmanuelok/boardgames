# Base URL of the billing/entitlements backend (see /serverless).
# Leave unset to run fully client-side: checkout shows an honest "not connected"
# note instead of redirecting to Stripe, and no entitlements are fetched.
#
# Example: VITE_API_BASE=https://api.your-domain.com
VITE_API_BASE=
