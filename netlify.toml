[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback so any direct URL serves the app (harmless with HashRouter,
# and correct if the router is ever switched to history mode).
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
